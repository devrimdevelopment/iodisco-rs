[package]
name = "gpuinfo-minimal"
version = "0.1.0"
edition = "2021"
description = "Minimal GPU information tool using iodisco"
license = "MIT OR Apache-2.0"
publish = false

[[bin]]
name = "gpuinfo"
path = "src/main.rs"

[dependencies]
iodisco = { path = "../iodisco", features = ["mali"] }
